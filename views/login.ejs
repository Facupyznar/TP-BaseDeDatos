<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8"/>
    <title>Login</title>
    <link rel="stylesheet" href="/styles.css"><!-- o /css/app.css si lo usás -->
</head>
<body>
<main class="container" style="max-width:420px;margin:2rem auto;">
    <h1 class="h3 mb-3 text-center">Iniciar sesión</h1>

    <% if (typeof error !== 'undefined' && error) { %>
        <div class="card" style="border:1px solid #f99;padding:.75rem;margin-bottom:1rem;color:#b00;">
            <%= error %>
        </div>
    <% } %>

    <form action="/login" method="POST" class="card" style="padding:1rem;">
        <div class="mb-3">
            <label for="username">Usuario</label>
            <input type="text" class="form-control" id="username" name="username" required/>
        </div>

        <div class="mb-3">
            <label for="password">Contraseña</label>
            <input type="password" class="form-control" id="password" name="password" required/>
        </div>

        <button class="btn btn-primary" style="width:100%;" type="submit">Iniciar sesión</button>

        <div class="mt-3 text-center">
            <a href="/register">¿No tenés cuenta? Registrate</a>
        </div>
    </form>
</main>
</body>
</html>
