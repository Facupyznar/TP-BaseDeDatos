<!DOCTYPE html>
<html>
<head>
    <title>MovieWeb</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f5f5f5;
            display: flex;
            flex-direction: column;
            justify-content: start;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding-top: 24px;
        }
        header {
            width: 100%;
            max-width: 780px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 16px;
            padding: 0 12px;
            box-sizing: border-box;
        }
        .actions a, .actions button {
            display: inline-block;
            padding: 8px 14px;
            font-size: 14px;
            border-radius: 6px;
            text-decoration: none;
            border: 1px solid #0078d4;
            background: #0078d4;
            color: #fff;
            cursor: pointer;
        }
        .actions a.secondary {
            background: transparent;
            color: #0078d4;
        }
        .actions form { display: inline; margin: 0; }
        h1 {
            font-size: 36px;
            color: #333333;
            margin: 12px 0 20px 0;
            text-align: center;
        }
        form.search {
            text-align: center;
        }
        input[type="text"] {
            padding: 10px;
            width: 300px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 18px;
            margin-right: 10px;
            background: #fff;
        }
        button[type="submit"] {
            background-color: #0078d4;
            color: white;
            border: none;
            border-radius: 4px;
            padding: 10px 20px;
            font-size: 18px;
            cursor: pointer;
        }
        button[type="submit"]:hover {
            background-color: #0056b3;
        }
        .hello { color:#333; font-size:14px; margin-right:8px; }
    </style>
</head>
<body>

<header>
    <div class="logo"><strong>MovieWeb</strong></div>

    <div class="actions">
        <% if (typeof user !== 'undefined' && user) { %>
            <span class="usuario"> <strong><%= user.username %></strong></span>
            <form action="/logout" method="POST">
                <button type="submit">Salir</button>
            </form>
        <% } else { %>
            <a href="/login">Iniciar sesión</a>
            <a class="secondary" href="/register">Registrate</a>
        <% } %>
    </div>
</header>

<h1>MovieWeb</h1>

<form class="search" action="/buscar" method="GET">
    <input type="text" name="q" placeholder="Buscar películas..." />
    <button type="submit">Buscar</button>
</form>

</body>
</html>
